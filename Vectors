#include<iostream>
#include<vector>
using namespace std;
int main()
{
    vector <int> v;
    v.push_back(19);
    v.push_back(20);
    v.push_back(21);
    v.push_back(22);
    v.pop_back();
    cout<<v[0]<<endl;
    cout<<v[1]<<endl;
    cout<<v[2]<<endl;
    cout<<v[3]<<endl;
     cout<<v.at(5)<<endl;
    vector <int> v2 (3,100);
    swap(v,v2);
     for(auto element:v)
    {
        cout<<element<<endl;
    } 
    for(auto element:v2)
    {
        cout<<element<<endl;
    }
    /*for(int i=0;i<v.size();i++)
    {
        int element;
        cin>>element;
        v.push_back(element);
    }
    
    for(auto element:v)
    {
        cout<<element;
    }*/
    
}

